# Deployment Architecture

OmniAgent AI supports:
- Local / On-premise Docker Compose deployment
- Cloud containerized deployment (AWS ECS / Fargate)
- Static CDN frontend hosting (Vercel / Cloudflare Pages)
